var Header = require('./header');

var Overview = React.createClass({
    render: function() {
        return (
            <Header title="Overview" />
        );
    }
});

module.exports = Overview;
