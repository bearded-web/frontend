var React = require('react');

var ScanReport = React.createClass({
    render: function() {
        return (
            <div className="c-scan-report">
                This is a scan-report component, fill render method with cool stufff.
            </div>
        );
    }
});

module.exports = ScanReport;
