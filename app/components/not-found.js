'use strict';
var NotFound = React.createClass({
    render: function() {
        return (
            <div >
            {iget('Page not found')}
            </div>
        );
    }
});

module.exports = NotFound;
